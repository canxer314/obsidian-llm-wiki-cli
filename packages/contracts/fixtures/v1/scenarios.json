{
  "contractVersion": "1.0.0",
  "scenarios": [
    {
      "id": "matching-identity-observed-health",
      "authority": "runtime",
      "expectation": "A matching connection header can initialize and observe this endpoint's health."
    },
    {
      "id": "missing-identity-rejected",
      "authority": "runtime",
      "expectation": "Initialization and every tool entry reject a missing expected Vault ID before dispatch."
    },
    {
      "id": "mismatched-identity-rejected",
      "authority": "runtime",
      "expectation": "Initialization and every tool entry reject a mismatched expected Vault ID without read-only degradation."
    },
    {
      "id": "schema-compatible-incompatible-health",
      "authority": "contract-and-runtime",
      "expectation": "Only the minimal incompatible health projection remains available and uses isError false."
    },
    {
      "id": "two-vault-coexistence",
      "authority": "installed-runtime",
      "expectation": "Two open Managed Vaults keep identity, endpoint, state, and health evidence isolated."
    },
    {
      "id": "structured-compatibility-identity",
      "authority": "contract-and-runtime",
      "expectation": "Compatibility text parses to the exact authoritative structuredContent value."
    },
    {
      "id": "byte-exact-ordered-mixed-read",
      "authority": "contract-and-runtime",
      "expectation": "One heterogeneous read preserves duplicate request indices, exact UTF-8 bytes, BOM, newline spelling, CJK, astral Unicode, and canonical Content Versions."
    },
    {
      "id": "section-occurrence-no-fallback",
      "authority": "contract-and-runtime",
      "expectation": "A complete heading hierarchy with an unsatisfied one-based occurrence returns not_satisfied without alternate content."
    },
    {
      "id": "exact-read-limit-and-grouping",
      "authority": "contract-and-runtime",
      "expectation": "A note over 1 MiB returns the typed limit result; aggregate Exact Reads over 1 MiB return only deterministic ordered contiguous groups."
    },
    {
      "id": "content-read-operational-block",
      "authority": "runtime",
      "expectation": "A content-blocking operational gate returns operationally_blocked before any raw byte access."
    },
    {
      "id": "invalid-utf8-no-trusted-result",
      "authority": "runtime",
      "expectation": "Invalid UTF-8 produces no trustworthy vault_read product result and never becomes not_satisfied."
    },
    {
      "id": "change-set-same-key-replay",
      "authority": "contract-and-runtime",
      "fixture": "vault-change-set/scenario-replay.json",
      "expectation": "The same Submission Key and canonical request return the durable Change Set identity and current proof without duplicate execution."
    },
    {
      "id": "change-set-key-conflict",
      "authority": "contract-and-runtime",
      "fixture": "vault-change-set/scenario-conflict.json",
      "expectation": "The same Submission Key with different canonical content returns only submission_key_conflict and preserves the existing record."
    },
    {
      "id": "change-set-preflight-rejection",
      "authority": "contract-and-runtime",
      "fixture": "vault-change-set/scenario-rejection.json",
      "expectation": "Canonical path, protected boundary, stale observation, absence, and exact-match failures register intent_not_applied before any mutation."
    },
    {
      "id": "change-set-uncertain-response-recovery",
      "authority": "runtime",
      "fixture": "vault-change-set/scenario-uncertain-response.json",
      "expectation": "After a lost or untrusted submit response, status by the original Submission Key recovers found, unknown, or expired knowledge without changing keys."
    },
    {
      "id": "change-set-seven-day-retention",
      "authority": "runtime",
      "expectation": "Accepted keys and complete records survive disconnect and restart for at least seven days, then retain expired tombstones."
    },
    {
      "id": "frozen-byte-exact-continuation",
      "authority": "contract-and-runtime",
      "expectation": "Every compact continuation page is within 256 KiB and contiguous per-index UTF-8 ranges reconstruct the accepted frozen Exact Read after Vault bytes change."
    },
    {
      "id": "single-use-client-bound-sliding-continuation",
      "authority": "runtime",
      "expectation": "Every token is single-use and client-bound; each replacement starts a new 15-minute lifetime while consumed, expired, malformed, and wrong-client tokens expose only continuation_unavailable."
    },
    {
      "id": "continuation-quota-and-lifecycle-cleanup",
      "authority": "runtime",
      "expectation": "Eight live chains or 8 MiB retained bytes reject new issuance without eviction, and completion, expiry, rejection, session teardown, and Bridge teardown immediately release capacity."
    },
    {
      "id": "continuation-operational-gate-precedence",
      "authority": "contract-and-runtime",
      "expectation": "A content-blocking effective gate returns operationally_blocked before inspecting or consuming a continuation token."
    },
    {
      "id": "structured-graph-discovery-composition",
      "authority": "contract-and-installed-runtime",
      "fixture": "vault-discover/valid/structured-graph-projection.json",
      "expectation": "Typed Frontmatter, tag, registered-reference, backlink, unresolved-link, graph, path, and text predicates compose over one immutable Search Snapshot with combined projections."
    },
    {
      "id": "registered-reference-byte-verification",
      "authority": "installed-runtime",
      "expectation": "Only installed-runtime-proven reference profiles are accepted, and every UTF-16 cache locator maps uniquely to matching raw UTF-8 bytes while preserving original syntax."
    },
    {
      "id": "successor-search-snapshot-graph-evidence",
      "authority": "installed-runtime",
      "expectation": "Rename, deletion, target creation, and unresolved-link changes publish coherent successor graph evidence while existing continuations retain their frozen predecessor result."
    }
  ]
}
